# Core dependencies
python-dotenv==1.0.0
requests==2.31.0

# SMS service
twilio==8.10.1

# AI summarization (choose one or both)
anthropic==0.39.0  # Claude API
google-generativeai>=0.8.0  # Gemini API (free tier available)

# RSS feed parsing
feedparser==6.0.11