# Twilio Configuration (Required)
# Get these from https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890  # Your Twilio phone number in E.164 format

# SMS Mode Configuration
# Choose how to split long messages:
# - 'segment': Split into 120-char segments (more SMS messages, better for basic plans)
# - 'long': Split into 1600-char segments (fewer messages, requires MMS support)
SMS_MODE=segment  # Options: segment (120 chars), long (1600 chars) (default: segment)

# AI Provider Configuration
# Choose your AI provider: 'claude' or 'gemini'
AI_PROVIDER=gemini  # Options: claude, gemini (default: claude)

# Claude API Configuration (Required if using Claude)
# Get your API key from https://console.anthropic.com/
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_MODEL=claude-3-5-sonnet-20241022  # Optional: claude-3-5-haiku-20241022 (cheaper), claude-3-5-sonnet-20241022

# Google Gemini API Configuration (Required if using Gemini)
# Get your API key from https://aistudio.google.com/apikey
# Gemini has a generous free tier: 15 requests/min, 1M tokens/min, 1500 requests/day
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp  # Optional: gemini-2.0-flash-exp (latest), gemini-1.5-flash